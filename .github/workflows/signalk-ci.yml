# ─────────────────────────────────────────────────────────────
# SignalK Plugin CI
#
# Calls the shared SignalK reusable workflow which tests this
# plugin across Linux x64, Linux arm64, macOS, and Windows on
# Node 22 + 24.
#
# enable-signalk-integration is OFF for the auto-run job: the
# upstream harness installs the plugin tarball into a fresh
# signalk-server and asserts it appears in /skServer/plugins.
# signalk-opencpn declares signalk-container as a hard runtime
# dependency in `signalk.requires`, and the harness has no way
# to install companion plugins listed there — so the plugin
# never gets a working __signalk_containerManager and the
# check fails structurally, not because of any real bug.
# Re-enable via the manual workflow_dispatch when an upstream
# `companion-plugins` input lands.
#
# armv7/Cerbo GX is OFF too — opt in via the manual trigger.
# ─────────────────────────────────────────────────────────────

name: SignalK Plugin CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      test-command:
        description: Command to run tests
        required: false
        default: npm test
        type: string
      build-command:
        description: Command to build the plugin
        required: false
        default: npm run build --if-present
        type: string
      format-check-command:
        description: 'Optional blocking format check, e.g. npm run format:check'
        required: false
        default: ''
        type: string
      coverage-command:
        description: 'Optional coverage command (replaces the test run)'
        required: false
        default: ''
        type: string
      node-versions:
        description: 'JSON array of Node versions, example: ["22","24"]'
        required: false
        default: '["22", "24"]'
        type: string
      enable-armv7:
        description: Run armv7 (Cerbo GX) tests via QEMU
        required: false
        default: false
        type: boolean
      enable-signalk-integration:
        description: Start SignalK server and install plugin for integration testing
        required: false
        default: true
        type: boolean
      signalk-server-versions:
        description: 'JSON array of signalk-server versions for integration testing'
        required: false
        default: '["latest"]'
        type: string

jobs:
  # ── Automatic runs (push / pull request) ──────────────────
  test-plugin-auto:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    uses: SignalK/signalk-server/.github/workflows/plugin-ci.yml@master
    with:
      test-command: npm test
      build-command: npm run build --if-present
      format-check-command: npm run format:check
      node-versions: '["22", "24"]'
      enable-armv7: false
      enable-signalk-integration: false
      signalk-server-versions: '["latest"]'

  # ── Manual runs (workflow_dispatch with custom inputs) ────
  test-plugin-manual:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    uses: SignalK/signalk-server/.github/workflows/plugin-ci.yml@master
    with:
      test-command: ${{ inputs.test-command }}
      build-command: ${{ inputs.build-command }}
      format-check-command: ${{ inputs.format-check-command }}
      coverage-command: ${{ inputs.coverage-command }}
      node-versions: ${{ inputs.node-versions }}
      enable-armv7: ${{ inputs.enable-armv7 }}
      enable-signalk-integration: ${{ inputs.enable-signalk-integration }}
      signalk-server-versions: ${{ inputs.signalk-server-versions }}

  # ── Bundle guards ─────────────────────────────────────────
  # Two failure modes the unit tests cannot see, both of which ship green:
  #  - typebox leaking into the browser bundle (config schema imported as a
  #    value rather than type-only)
  #  - react/jsx-runtime appearing, which means the classic-runtime setting
  #    regressed and the panel will mount with a second React instance whose
  #    dispatcher is null.
  bundle-guards:
    if: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '24'

      - run: npm install

      - name: Build
        run: npm run build

      - name: No typebox in the browser bundle
        run: |
          if grep -rli typebox public/; then
            echo "::error::typebox leaked into the panel bundle — import config types as type-only"
            exit 1
          fi

      - name: No jsx-runtime in the browser bundle
        run: |
          if grep -rlo 'jsx-runtime' public/; then
            echo "::error::react/jsx-runtime in the bundle — jsxRuntime must stay 'classic'"
            exit 1
          fi

      - name: remoteEntry.js was emitted
        run: test -f public/remoteEntry.js
